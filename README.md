# Информационная система логистики

Веб-приложение для управления транспортными заказами, клиентами, транспортом и водителями. Система построена на Spring Boot 4 с использованием JWT-аутентификации и предоставляет как REST API, так и веб-интерфейс на Thymeleaf.

## Технологический стек

- **Java 17**
- **Spring Boot 4.0.0**
- **Spring Security 6+** (JWT-аутентификация)
- **Spring Data JPA**
- **PostgreSQL** (база данных)
- **Thymeleaf** (веб-интерфейс)
- **Bootstrap 5** (UI)
- **Lombok** (упрощение кода)
- **JJWT 0.12.3** (JWT токены)
- **Maven** (система сборки)

##  Функциональность

### Основные модули

- **Управление заказами** - создание, редактирование, отслеживание статусов транспортных заказов
- **Управление клиентами** - ведение базы компаний-клиентов
- **Управление транспортом** - учет транспортных средств с характеристиками
- **Управление водителями** - база водителей с лицензиями и опытом
- **Управление пользователями** - система ролей (ADMIN, MANAGER, OPERATOR)
- **Статистика** - отчеты по заказам, клиентам и загрузке транспорта
- **История статусов** - отслеживание изменений статусов заказов
- **Назначения** - привязка водителей и транспорта к заказам

### Безопасность

- JWT-токены для REST API
- Ролевая модель доступа (ADMIN, MANAGER, OPERATOR)
- BCrypt хеширование паролей
- Защита REST API с сохранением публичного доступа к веб-интерфейсу

##  Архитектура

Проект следует многослойной архитектуре:

```
org.logistservice.logist/
├── config/          # Конфигурация приложения, CORS, инициализация данных
├── security/        # JWT, аутентификация, авторизация
├── common/          # Общие DTO, исключения, обработка ошибок
├── user/            # Пользователи и роли
│   ├── model/       # User, Role, RoleName
│   ├── repository/  # UserRepository, RoleRepository
│   ├── service/     # UserService
│   └── controller/  # UserController (REST)
├── client/          # Клиенты
├── vehicle/         # Транспорт
├── driver/          # Водители
├── order/           # Заказы
│   ├── model/       # Order, OrderAssignment, OrderStatusHistory, DTOs
│   ├── repository/  # OrderRepository, OrderAssignmentRepository, OrderStatusHistoryRepository
│   ├── service/     # OrderService
│   └── controller/  # OrderController (REST)
├── stats/           # Статистика
├── about/           # Информация об авторе
└── ui/              # Веб-интерфейс (Thymeleaf)
    └── controller/  # UI контроллеры
```

##  Установка и запуск

### Требования

- Java 17 или выше
- Maven 3.6+
- PostgreSQL 12+

### Настройка базы данных

1. Создайте базу данных PostgreSQL:
```sql
CREATE DATABASE logist_db;
```

2. Обновите настройки подключения в `src/main/resources/application.properties`:
```properties
spring.datasource.url=jdbc:postgresql://localhost:5432/logist_db
spring.datasource.username=your_username
spring.datasource.password=your_password
```

### Запуск приложения

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd logist
```

2. Соберите проект:
```bash
./mvnw clean install
```

3. Запустите приложение:
```bash
./mvnw spring-boot:run
```

Или используйте IDE для запуска класса `LogistApplication`.

4. Приложение будет доступно по адресу: `http://localhost:8080`

### Инициализация данных

При первом запуске автоматически создаются:
- Роли: ADMIN, MANAGER, OPERATOR
- Пользователь-администратор:
  - Логин: `admin`
  - Пароль: `admin`
  - Роль: ADMIN

**Важно**: Измените пароль администратора в продакшене!

## Аутентификация

### REST API

REST API использует JWT-токены. Для доступа к защищенным эндпоинтам:

1. Зарегистрируйтесь или войдите:
```bash
POST /api/auth/register
{
  "username": "user",
  "password": "password",
  "fullName": "Full Name",
  "email": "user@example.com"
}

POST /api/auth/login
{
  "username": "user",
  "password": "password"
}
```

2. Используйте полученный токен в заголовке:
```
Authorization: Bearer <token>
```

### Веб-интерфейс

Веб-интерфейс доступен без аутентификации для демонстрации:
- Главная страница: `http://localhost:8080/`
- Заказы: `http://localhost:8080/ui/orders`
- Клиенты: `http://localhost:8080/ui/clients`
- Транспорт: `http://localhost:8080/ui/vehicles`
- Водители: `http://localhost:8080/ui/drivers`
- Статистика: `http://localhost:8080/ui/stats`
- Об авторе: `http://localhost:8080/ui/about`

## 📡 REST API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация нового пользователя
- `POST /api/auth/login` - Вход в систему

### Заказы
- `GET /api/orders` - Список заказов (с фильтрами)
- `GET /api/orders/{id}` - Детали заказа
- `POST /api/orders` - Создание заказа
- `PUT /api/orders/{id}` - Обновление заказа
- `DELETE /api/orders/{id}` - Удаление заказа
- `PATCH /api/orders/{id}/status` - Изменение статуса заказа
- `POST /api/orders/{orderId}/assignments` - Добавление назначения
- `PUT /api/orders/assignments/{assignmentId}` - Обновление назначения
- `DELETE /api/orders/assignments/{assignmentId}` - Удаление назначения

### Клиенты
- `GET /api/clients` - Список клиентов
- `GET /api/clients/{id}` - Детали клиента
- `POST /api/clients` - Создание клиента
- `PUT /api/clients/{id}` - Обновление клиента
- `DELETE /api/clients/{id}` - Удаление клиента

### Транспорт
- `GET /api/vehicles` - Список транспорта
- `GET /api/vehicles/{id}` - Детали транспорта
- `POST /api/vehicles` - Создание транспорта
- `PUT /api/vehicles/{id}` - Обновление транспорта
- `DELETE /api/vehicles/{id}` - Удаление транспорта

### Водители
- `GET /api/drivers` - Список водителей
- `GET /api/drivers/{id}` - Детали водителя
- `POST /api/drivers` - Создание водителя
- `PUT /api/drivers/{id}` - Обновление водителя
- `DELETE /api/drivers/{id}` - Удаление водителя

### Статистика
- `GET /api/stats/order-status` - Статистика по статусам заказов
- `GET /api/stats/top-clients` - Топ клиентов
- `GET /api/stats/vehicle-load` - Загрузка транспорта

### Информация
- `GET /api/about` - Информация об авторе

## Роли и права доступа

- **ADMIN** - Полный доступ ко всем операциям
- **MANAGER** - Чтение и запись (создание, редактирование, удаление)
- **OPERATOR** - Только чтение

REST API защищен JWT-токенами. Веб-интерфейс доступен публично для демонстрации.

## Модель данных

### Основные сущности

- **User** - Пользователи системы
- **Role** - Роли (ADMIN, MANAGER, OPERATOR)
- **Client** - Компании-клиенты
- **Vehicle** - Транспортные средства
- **Driver** - Водители
- **Order** - Транспортные заказы
- **OrderAssignment** - Назначения водителей и транспорта на заказы
- **OrderStatusHistory** - История изменений статусов заказов

### Статусы заказов

- `NEW` - Новый заказ
- `IN_PROGRESS` - В процессе выполнения
- `DELIVERED` - Доставлен
- `CANCELED` - Отменен

### Статусы транспорта

- `ACTIVE` - Активен
- `IN_SERVICE` - В обслуживании
- `OUT_OF_SERVICE` - Не в эксплуатации

## 🛠️ Разработка

### Структура проекта

```
src/
├── main/
│   ├── java/
│   │   └── org/logistservice/logist/
│   │       ├── [domain]/          # Доменные пакеты
│   │       │   ├── model/         # JPA сущности и DTOs
│   │       │   ├── repository/   # JPA репозитории
│   │       │   ├── service/       # Бизнес-логика
│   │       │   └── controller/    # REST контроллеры
│   │       ├── ui/controller/     # UI контроллеры (Thymeleaf)
│   │       ├── security/          # Безопасность
│   │       ├── config/            # Конфигурация
│   │       └── common/            # Общие компоненты
│   └── resources/
│       ├── application.properties # Настройки приложения
│       ├── static/                # Статические ресурсы (CSS, JS)
│       └── templates/             # Thymeleaf шаблоны
└── test/                          # Тесты
```

### Настройки JWT

В `application.properties`:
```properties
app.jwt.secret=your-secret-key-here
app.jwt.expiration-ms=3600000  # 1 час
```

**Важно**: Измените секретный ключ JWT в продакшене!

## Тестирование

Запуск тестов:
```bash
./mvnw test
```

## Логирование

Настройки логирования в `application.properties`:
- SQL запросы: `DEBUG`
- Spring Security: `INFO`

##  Обработка ошибок

Система использует глобальный обработчик исключений (`GlobalExceptionHandler`), который возвращает структурированные ответы об ошибках:

```json
{
  "timestamp": "2024-01-01T12:00:00",
  "status": 404,
  "error": "Not Found",
  "message": "Order not found with id: 1",
  "path": "/api/orders/1"
}
```

## Лицензия

Этот проект создан в учебных целях.

##  Автор

Информация об авторе доступна по адресу: `/api/about` или `/ui/about`

##  Версии

- **v0.0.1-SNAPSHOT** - Текущая версия

---

